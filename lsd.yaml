blocks:
  # - git
  - name
  - size
  - date

color:
  when: auto
  theme: default

icons:
  when: never

indicators: true

sorting:
  dir-grouping: first

# total-size: true

# hyperlink: always

header: false

